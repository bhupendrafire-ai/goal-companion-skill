interface:
  display_name: "Goal Companion"
  short_description: "Quietly refine long Codex goals when enabled."
  default_prompt: "Use $goal-companion in quiet mode to refine this long goal, creating a side thread or keepalive only if needed."
