interface:
  display_name: "Goal Companion"
  short_description: "Keep long Codex goals sharp with a side thread."
  default_prompt: "Use $goal-companion to create a side thread that refines this goal as the run proceeds."
